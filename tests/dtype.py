import torch

print(torch.zeros(1, dtype=bool))
print(torch.zeros(1, dtype=int))
print(torch.zeros(1, dtype=float))
print(torch.zeros(1, dtype=torch.uint8))
print(torch.zeros(1, dtype=torch.int8))
print(torch.zeros(1, dtype=torch.float64))
print(torch.zeros(1, dtype=torch.double))
print(torch.zeros(1, dtype=torch.float32))
print(torch.zeros(1, dtype=torch.float))
print(torch.zeros(1, dtype=torch.int32))
print(torch.zeros(1, dtype=torch.int))
print(torch.zeros(1, dtype=torch.int64))
print(torch.zeros(1, dtype=torch.long))
print(torch.zeros(1, dtype=torch.int16))
print(torch.zeros(1, dtype=torch.short))
print(torch.zeros(1, dtype=torch.float16))
print(torch.zeros(1, dtype=torch.half))
#print(torch.zeros(1, dtype=torch.complex32))
#print(torch.zeros(1, dtype=torch.complex64))
#print(torch.zeros(1, dtype=torch.cfloat))
#print(torch.zeros(1, dtype=torch.complex128))
#print(torch.zeros(1, dtype=torch.cdouble))
print(torch.zeros(1, dtype=torch.bool))
#print(torch.zeros(1, dtype=torch.qint8))
#print(torch.zeros(1, dtype=torch.quint8))
#print(torch.zeros(1, dtype=torch.qint32))
print(torch.zeros(1, dtype=torch.bfloat16))
#print(torch.zeros(1, dtype=torch.quint4x2))
